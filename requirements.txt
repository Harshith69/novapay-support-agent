# --- Core runtime (app, agents, RAG, evaluation) ---
gradio>=4.0.0
python-dotenv>=1.0.0
# LLM providers (install the one you use; groq is the free default)
groq>=0.11.0
# anthropic>=0.25.0
# google-generativeai>=0.7.0   # for the gemini provider
# openai>=1.0.0                # for the ollama (local) provider

# --- RAG ---
chromadb>=0.5.0
sentence-transformers>=2.7.0
langchain>=0.2.0
langchain-community>=0.2.0
langchain-text-splitters>=0.2.0

# --- Evaluation ---
scikit-learn>=1.4.0
evaluate>=0.4.0
bert-score>=0.3.13

# --- Fine-tuning (GPU only; install on Colab/Kaggle, not needed for the Space) ---
# unsloth
# torch
# transformers>=4.40.0
# trl>=0.8.0
# peft>=0.10.0
# datasets>=2.19.0
